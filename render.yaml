services:
  - type: web
    name: carrier-app
    env: docker
    plan: starter        # or higher; any paid plan that supports disks
    envVars:
      - key: SQLITE_DB_PATH
        value: /data/shipments.db
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
